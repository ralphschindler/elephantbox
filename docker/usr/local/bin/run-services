#!/usr/bin/env bash

if [[ "$@" == *"cron"* ]]; then
  rm /etc/service/cron/down
fi

if [[ "$@" == *"nginx"* ]]; then
  rm /etc/service/nginx/down
fi

if [[ "$@" == *"php-fpm"* ]]; then
  rm /etc/service/php-fpm/down
fi

exec my_init
